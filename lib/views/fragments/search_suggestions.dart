import 'package:flutter/material.dart';

class SearchSuggestionsFragment extends StatelessWidget {
  const SearchSuggestionsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
